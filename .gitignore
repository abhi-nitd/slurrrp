# Local-only files — do NOT upload these to GitHub / the host.

# Your local database + secret signing key
data/

# cloudflared.exe (Windows tunnel tool, not needed online)
tools/

# Python cache
__pycache__/
*.pyc

# Logs
*.log

# Local preview config
.claude/
